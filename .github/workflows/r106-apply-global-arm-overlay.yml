name: r106-apply-global-arm-overlay

on:
  push:
    branches:
      - rgb/r10.6-pro-ui-stabilization-20260830
    paths:
      - patches/r106_global_arm_overlay.py
      - .github/workflows/r106-apply-global-arm-overlay.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: rgb/r10.6-pro-ui-stabilization-20260830
          fetch-depth: 0
          persist-credentials: true
      - name: Apply global ARM and overlay fix
        run: |
          python patches/r106_global_arm_overlay.py
          git diff --check
          git config user.name "rgb-estudios-r10.6-bot"
          git config user.email "actions@users.noreply.github.com"
          git add product/AeylaVisualDmx/AeylaRuntimeStatusControl.h
          git rm patches/r106_global_arm_overlay.py
          git rm .github/workflows/r106-apply-global-arm-overlay.yml
          git diff --cached --quiet && { echo "No R10.6 interaction changes produced"; exit 1; }
          git commit -m "fix(r10.6): make global ARM and overlays interaction-safe"
          git push origin HEAD:rgb/r10.6-pro-ui-stabilization-20260830
